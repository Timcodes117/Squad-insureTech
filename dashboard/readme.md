# dashboard/

This folder hosts the BetaHealth web apps.

- **`admin/`** — admin operations console (Next.js + Tailwind). See [admin/README.md](admin/README.md) for setup. Run from there:
  ```bash
  cd admin
  npm install
  npm run dev
  ```

Future apps (hospital portal, customer portal) would sit alongside `admin/` as siblings.
