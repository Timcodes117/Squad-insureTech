import { apiClient } from '@/core/api/client';

export const onboardingApi = {
  // TODO: persist onboarding selections via Node.js backend when required.
  saveProgress: async (): Promise<void> => {
    void apiClient;
    return;
  },
};
