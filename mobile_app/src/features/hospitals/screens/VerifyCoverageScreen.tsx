import { ScreenPlaceholder } from '@/shared/components/ScreenPlaceholder';

/** Staff / hospital desk flow (scan member QR) — not used in member app MVP. */
export default function VerifyCoverageScreen() {
  return <ScreenPlaceholder title="Verify coverage (staff)" />;
}
